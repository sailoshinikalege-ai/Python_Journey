# ============================================================
#              PYTHON LAMBDA – ASSESSMENT
#                    20 QUESTIONS
# ============================================================

# 1. WAP using lambda to find the square of a number.
square = lambda n : n *n
print(square(3))

# 2. WAP using lambda to find the cube of a number.
cube = lambda m : m*m*m
print(cube(3))

# 3. WAP using lambda to add two numbers.
add = lambda a,b:a+b
print(add(2,3))

# 4. WAP using lambda to subtract two numbers.
sub = lambda x,y:x-y
print(sub(5,4))

# 5. WAP using lambda to multiply two numbers.
mul = lambda c,d:c*d
print(mul(4,2))

# 6. WAP using lambda to divide two numbers.
div = lambda a,b : a/b
print(div(6,3))

# 7. WAP using lambda to find the greater of two numbers.
great = lambda m,n:m if m>n else n
print ('the greatest:',great(5,6))

# 8. WAP using lambda to find the smaller of two numbers.
small = lambda c ,d :c if c<d else d
print('Smallest:',small(5,3))

# 9. WAP using lambda to check whether a number is even.
even = lambda i :i if  i%2 ==0 else None
print(even(8))

# 10. WAP using lambda to check whether a number is odd.
odd = lambda i :i if  i%2 !=0 else None
print(odd(8))

# 11. WAP using lambda to find the maximum of three numbers.
num = lambda a,b,c : max (a,b,c)
print(num(5,4,3))

# 12. WAP using lambda to find the minimum of three numbers.


# 13. WAP using lambda to calculate the area of a rectangle.


# 14. WAP using lambda to calculate the area of a circle.


# 15. WAP using lambda to calculate simple interest.


# 16. WAP using lambda to calculate the average of three numbers.


# 17. WAP using lambda to convert Celsius to Fahrenheit.


# 18. WAP using lambda to convert Fahrenheit to Celsius.


# 19. WAP using lambda to check whether a number is positive or negative.


# 20. WAP using lambda to calculate the percentage of five subject marks.